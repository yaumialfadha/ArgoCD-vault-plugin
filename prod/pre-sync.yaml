apiVersion: batch/v1
kind: Job
metadata:
  generateName: Pre-Sync-job
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
        - name: Pre-Sync-job
          image: alpine:latest
          command: ["sleep", "5"]
      restartPolicy: Never
  
